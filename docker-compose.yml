services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: cataclysmapp:latest
    ports:
      - "8000:8000"
    environment:
      PORT: ${PORT:-8000}
      DEFAULT_USERNAME: ${DEFAULT_USERNAME:-admin}
      DEFAULT_PASSWORD: ${DEFAULT_PASSWORD:?Set DEFAULT_PASSWORD in your environment or .env file}
