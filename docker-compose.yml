services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: cataclysmapp:latest
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
      DEFAULT_USERNAME: admin
      DEFAULT_PASSWORD: ChangeMeToAStrongPassword
